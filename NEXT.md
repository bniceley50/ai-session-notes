# NEXT (Only the next 1-3 actions)

1) Add Supabase client + env skeleton (no secrets committed).
   Verify: `git status -sb` shows only expected changes and app still boots (`pnpm dev`).

Completed: Initialized Supabase local folder (Test-Path .\supabase\config.toml is True).
